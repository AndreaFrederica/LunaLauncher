--- a/meson.build
+++ b/meson.build
@@ -342,16 +342,11 @@ zstd_dep = dependency(
 if zstd_dep.found()
   cdata.set('HAVE_LIBZSTD', 1)
   cdata.set('HAVE_ZSTD_H', 1)
   deps += zstd_dep
-  foreach sym : ['ZSTD_compressStream', 'ZSTD_minCLevel']
-    if cc.has_header_symbol(
-      'zstd.h',
-      sym,
-      dependencies: zstd_dep,
-    )
-      cdata.set('HAVE_' + sym, 1)
-    endif
-  endforeach
+  cdata.set('HAVE_ZSTD_compressStream', 1)
+  if zstd_dep.version().version_compare('>=1.3.4')
+    cdata.set('HAVE_ZSTD_minCLevel', 1)
+  endif
 endif
 
 lzma_dep = dependency(
--- a/libarchive/meson.build
+++ b/libarchive/meson.build
@@ -175,7 +175,8 @@ libarchive = library(
 depinc = include_directories('.')
 libarchive_dep = declare_dependency(
   include_directories: depinc,
   link_with: libarchive,
+  compile_args: host_machine.system() == 'windows' and get_option('default_library') == 'static' ? ['-DLIBARCHIVE_STATIC'] : [],
 )
 meson.override_dependency('libarchive', libarchive_dep)
 
